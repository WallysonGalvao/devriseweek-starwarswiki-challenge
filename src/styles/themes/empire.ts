import { colors } from '../colors';
import { fonts } from '../fonts';
import { metrics } from '../metrics';

export const empire = {
  colors: { ...colors, primary: '#F44336' },
  fonts,
  metrics,
};
